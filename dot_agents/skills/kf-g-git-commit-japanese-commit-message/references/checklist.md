# 最終チェック

- 1行目が `<type_>: <カテゴリ>_<subject>` になっている
- 2行目が空行になっている
- 3行目以降に本文がある
- prefix が許可された 8 種類のどれかで、5 文字固定になっている
- 直近 10 commit の 1 行目を確認し、必要なら既存の type_ や subject を流用できている
- docs repo なら `docs_` を選ばず、変更の性質に応じた prefix を選べている
- プロジェクトの動作に直接影響しない変更で `feat_` を使っていない
- subject が日本語で簡潔に書かれている
- subject の先頭に `<カテゴリ>_` が付いている
- カテゴリが簡潔で、変更対象を表せている
- subject が変更の主目的を表している
- 本文に `- 概要` と `- Why:` が含まれている
- `概要` と `Why` が、必要に応じて複数行の箇条書きに分けられている
- 全体が端的で、長文は箇条書きへ分割されている
- 体言止めを多用し、敬語を使っていない
- 本文が日本語で簡潔に書かれている
